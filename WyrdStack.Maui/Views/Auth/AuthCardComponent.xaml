<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:WyrdStack.Maui.ViewModels.Auth"
             x:Class="WyrdStack.Maui.Views.Auth.AuthCardComponent"
             x:DataType="vm:AuthCardComponentViewModel">
    <Border 
    Background="White" 
    Stroke="#E1E4E8" 
    StrokeThickness="1" 
    StrokeShape="RoundRectangle 12" 
    Margin="20" Padding="20"
    x:Name="AuthBorder"
    HorizontalOptions="Center"
    VerticalOptions="Center" >

        <Border.Shadow>
            <Shadow Brush="Black" Offset="0,4" Radius="10" Opacity="0.1" />
        </Border.Shadow>


        <VerticalStackLayout Padding="0,20" Spacing="15">

            <Label Text="{Binding Title}" 
                   FontSize="28" 
                   FontAttributes="Bold" 
                   TextColor="#2D3436"
                   HorizontalOptions="Center" />

            <Entry x:Name="EmailEntry" 
                   Placeholder="Email" 
                   MaxLength="64" 
                   BackgroundColor="#F8F9FA"
                   TextColor="#2D3436"
                   Text="{Binding Email}"/>
            <Entry x:Name="UsernameEntry" 
                   Placeholder="Username" 
                   MaxLength="64" 
                   BackgroundColor="#F8F9FA"
                   TextColor="#2D3436"
                   IsVisible="{Binding HasUsernameEntry}"
                   Text="{Binding Username}"/>

            <Entry x:Name="PasswordEntry" 
                   Placeholder="Password" 
                   IsPassword="{Binding IsPassword}" 
                   BackgroundColor="#F8F9FA"
                   TextColor="#2D3436"
                   Text="{Binding Password}" 
                   MaxLength="64" />

            <HorizontalStackLayout Spacing="8">
                <CheckBox x:Name="ShowPasswordCheckBox" 
                          Color="#0984E3"
                          Command="{Binding TogglePasswordVisibilityCommand}" />
                <Label Text="Show Password" 
                       TextColor="#636E72"
                       VerticalOptions="Center" />
            </HorizontalStackLayout>

            <Button Text="{Binding ActionButtonText}" 
                    Command="{Binding ExecuteActionButtonCommand}" 
                    CommandParameter="{Binding Source={x:Reference EmailEntry}, Path=Text}"
                    x:Name="ActionButton"
                    BackgroundColor="#0984E3"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="45"
                    FontAttributes="Bold">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="PointerOver">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#74b9ff" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Pressed">
                            <VisualState.Setters>
                                <Setter Property="Scale" Value="0.98" />
                                <Setter Property="BackgroundColor" Value="#0984E3" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
            </Button>

            <Label Text="{Binding StatusMessage}" 
                   TextColor="#D63031" 
                   HorizontalOptions="Center" 
                   FontSize="12"/>
            <ActivityIndicator IsRunning="{Binding IsLoading}" 
                   IsVisible="{Binding IsLoading}" 
                   Color="#0984E3" 
                   HeightRequest="40" 
                   HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Border>
</ContentView>
